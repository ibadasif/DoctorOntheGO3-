<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.doctoronthego3">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Mah"
            android:label="@string/title_activity_mah"></activity>
        <activity
            android:name=".Ahc"
            android:label="@string/title_activity_ahc" />
        <activity
            android:name=".Dsm"
            android:label="@string/title_activity_dsm" />
        <activity
            android:name=".Kmc"
            android:label="@string/title_activity_kmc" />
        <activity
            android:name=".Taj"
            android:label="@string/title_activity_taj" />
        <activity
            android:name=".Camps"
            android:label="@string/title_activity_camps" />
        <activity
            android:name=".Heah"
            android:label="@string/title_activity_heah" />
        <activity
            android:name=".Dow"
            android:label="@string/title_activity_dow" />
        <activity
            android:name=".Aku"
            android:label="@string/title_activity_aku" />
        <activity android:name=".Signup" />
        <activity android:name=".LoginPage" />
        <activity
            android:name=".HosMap"
            android:label="@string/title_activity_hos_map" />

        <activity android:name=".Maps" />

        <activity android:name=".Doctors" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Pedometer" />
        <activity android:name=".Diagnose" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>